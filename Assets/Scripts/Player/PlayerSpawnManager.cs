// PlayerSpawnManager.cs (Solo código, no en la escena)
public static class PlayerSpawnManager
{
    // Guarda el ID del punto de entrada donde debe aparecer el jugador
    public static string entryPointID = null;
}